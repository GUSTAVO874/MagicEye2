@page "/"
@using MagicEye2.RichUI.Models
@using MagicEye2.RichUI.Data


<PageTitle>Home</PageTitle>

<h1>Hello, world!</h1>
@* <RadzenDataGrid AllowFiltering="true" AllowColumnResize="true" AllowAlternatingRows="false" FilterMode="FilterMode.Advanced" AllowSorting="true" PageSize="5" AllowPaging="true" PagerHorizontalAlign="HorizontalAlign.Left" ShowPagingSummary="true"
                Data="@maestrotbenef" ColumnWidth="300px" LogicalFilterOperator="LogicalFilterOperator.Or" SelectionMode="DataGridSelectionMode.Single" @bind-Value=@selectedmaestrotbenef>
    <Columns>
        <RadzenDataGridColumn Property="@nameof(MaestroTBeneficiario.Codigo)" Filterable="false" Title="ID" Frozen="true" Width="80px" TextAlign="TextAlign.Center" />
        <RadzenDataGridColumn Title="Photo" Frozen="true" Sortable="false" Filterable="false" Width="80px" TextAlign="TextAlign.Center">
             <Template Context="data">
                <RadzenImage Path="@data.Photo" class="rz-gravatar" AlternateText="@(data.FirstName + " " + data.LastName)" />
            </Template> 
        </RadzenDataGridColumn>
        <RadzenDataGridColumn Property="@nameof(MaestroTBeneficiario.MaestroTBeneficiarioId)" Title="First Name" Frozen="true" Width="160px" />
        <RadzenDataGridColumn Property="@nameof(MaestroTBeneficiario.Codigo)" Title="Last Name" Width="160px" />
        
    </Columns>
</RadzenDataGrid> *@

Welcome to your new app.
@code {
    IQueryable<MaestroTBeneficiario> maestrotbenef;
    IList<MaestroTBeneficiario> selectedmaestrotbenef;
    private readonly AppDbContext _db;
    MaestroTBeneficiario MaestroTBeneficiario = new MaestroTBeneficiario();

    protected override async Task OnInitializedAsync()
    {
        await base.OnInitializedAsync();
        
        
        maestrotbenef = _db.MaestroTBeneficiarios;

        selectedmaestrotbenef = new List<MaestroTBeneficiario>() { maestrotbenef.FirstOrDefault() };
    }
}
